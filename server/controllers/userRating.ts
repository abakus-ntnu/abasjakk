import User from "../models/userRating";
import { RequestHandler } from "express";

const createUserRating: RequestHandler = async (req, res) => {};

const getUserRating: RequestHandler = async (req, res) => {};

export default { createUserRating, getUserRating };
